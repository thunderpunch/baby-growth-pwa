# 宝宝成长记录 — Agent Rules

这是本仓库所有开发 Agent 的首要入口。开始修改前先读本文件，再按任务读取对应权威文档。

## 1. 产品定位

这是一个以 iPad / iPhone 主屏幕 PWA 为重要使用场景的 local-first 宝宝成长记录工具。

核心原则：

- 录入尽量简单，底层事实尽量完整，分析精度由问题决定。
- 数据安全优先于视觉更新、重装便利和技术炫技。
- 静态/default UI 可立即显示，异步模块原位 hydrate；禁止默认 UI → 空白 → 实际 UI。
- 页面应像稳定、克制的应用，不应暴露廉价的 Web 感；普通应用外壳禁止文本长按 callout，输入/可复制区域显式恢复。
- 用户不应被迫理解后端可以推断的概念；只有真正不可消除的歧义才要求显式选择。

## 2. 用户反复强调的工程要求

### 代码收敛 / 洁癖

- 不把新功能继续堆进单体文件。
- 一个业务能力只允许有一个当前实现和一个明确 owner。
- 新实现稳定后，旧实现应物理删除，不长期靠 hidden DOM、bridge、monkey patch 或重复 handler 维持。
- 兼容层只能是有明确删除条件的短期措施，并应写入 `MAINTENANCE.md`。
- 不为了“兼容”复制业务逻辑。
- 不为了减少文件数量机械合并模块；按职责边界组织。
- 清理顺序：不可达旧代码 → 运行时兼容层 → 大模块拆分 → 重复样式/工具 → 最后才考虑 Git 历史重写。

### 自动验证优先

发现可自动化的问题时，先补 regression / contract，再发布。

发布前必须运行 `node scripts/verify.mjs` 对应的 CI。

只有同时满足：

1. 最新 `static-check`：`completed / success`；
2. 最新 GitHub Pages：同一 HEAD，`completed / success`；

才允许对用户说“部署完成”。

不要把“已提交”“CI 在跑”“Pages queued”描述成部署完成。

### 不靠用户手测发现明显回归

优先在代码层验证：

- DOM/模块契约；
- 时间模型；
- 跨日/跨月/跨年；
- 缓存更新；
- repository hygiene；
- 性能约束；
- 数据迁移与导入导出。

## 3. 数据安全与长期保存

- 宝宝记录默认只存本机 IndexedDB，不自动上传 GitHub。
- 删除主屏幕 PWA、清浏览器网站数据、设备损坏都可能造成数据不可恢复；不要建议用户为了换图标直接重装而不先备份。
- JSON 是归档、恢复和分析的主格式；Excel 主要用于人工查看。
- 当前 JSON：schema `1.2.0`，`dataVersion = 3`，`timeModelVersion = 1`。
- `record-model.js` 是时间事实唯一模型；`createdAt/updatedAt` 不是事件发生时间。
- `date` 主要承担页面/IndexedDB 业务归档桶，不是唯一真实时间。

### 分析后的邮件备份工作流

当用户把记录数据交给 ChatGPT 分析时：

1. 校验本次数据；
2. 与对应自然年既有主数据合并；
3. 完成分析；
4. 生成该自然年“截至本次”的完整 JSON 快照，而不是仅新增量；
5. 通过用户已确认的邮箱发送该年度完整 JSON 备份。

按自然年分档，例如 `宝宝成长记录_2026.json`、`宝宝成长记录_2027.json`。不要把多年数据无限堆在单一生命周期文件里。

## 4. 自动待确认模板

- `record-templates.js` 是吃奶 / 饮食待确认模板的唯一 owner。
- 当天只要已经有一条已确认吃奶记录，就不再自动生成吃奶模板。
- 当天的尿布、辅食、睡眠、Context 等其它事实 **不能阻止吃奶模板生成**。
- 当天没有已确认吃奶且尚未处理过吃奶模板时，向前最多查 3 个自然日；取最近一个可作为来源的吃奶日期，并复制该日期的全部吃奶事实为当天待确认模板。
- 奶模板来源日必须已经形成“最终事实集合”：至少有 1 条已确认吃奶，且当天不能仍有任何未处理的吃奶 pending。只确认部分待确认吃奶时，该日暂时不能向下一天传播。
- 待确认吃奶全部确认/跳过后，剩余的 `confirmed milk` 才能成为后续模板来源；被跳过/删除的 pending 不再阻断来源判定。
- 3 天内都没有符合条件的吃奶来源日时不生成，也不扫描更久的历史。
- 待确认模板不计入正式统计；用户确认后才成为正式记录，选择“今天没有”后不能在同一天重复生成。
- 奶和饮食模板策略必须独立，不能共用一个开关导致其中一个阻断另一个。
- 不允许恢复 `recent-milk-template.js` 那种 DOM 监听 + `getAllRecords()` 全库扫描桥接实现。

## 5. 睡眠事实模型

- 只有一个 factual `sleep` 记录类型。
- 用户普通录入不选择“小睡 / 夜间主睡”；系统推断 nap/night/uncertain。
- 高置信度小睡才计入小睡统计；不确定段保持“待判断”。
- 普通睡眠允许跨午夜，UI 不要求用户选择日期。
- Good Night：记录睡着时间、入睡方式、备注；不要求醒来时间。
- Good Morning：完成昨夜睡眠；若没有 Good Night，则补睡着时间 + 醒来时间 + 入睡方式。
- `wake` 是独立 factual record；夜醒录入必须允许选择“属于昨晚 / 属于今晚”，用户选择是真相。
- 睡眠与睡眠重叠是需要提示的真实冲突；吃奶、尿布等发生在睡眠区间内不构成冲突。
- 首页可见顺序固定：早安 → 昨晚睡眠摘要 → 晚安 → 当天例外。
- 可见结构直接存在于 `index.html`；禁止恢复运行时 DOM bridge 或隐藏 legacy sleep mount。

## 6. History 长期浏览

History 的目标是多年可用，不是只服务最近几天。

当前模式：

- 默认“最近30天”；
- 可切换按月浏览；
- 上月/下月自然跨年；
- 当前年份在卡片/月份文案中可省略，非当前年份必须明确显示；
- 单次只通过日期 index 读取当前范围，不允许 `getAllRecords()/getAllDays()` 后前端筛选；
- 不恢复“最近 30 个有记录日期”的 `.slice(0,30)`；最近30天是连续日期范围；
- 不增加重复的 History 内“跳到日期”控件；具体日期跳转复用全局日期入口；
- 已删除“批量补录”入口，不恢复仅仅跳回 Today 的伪功能。

未来图表必须先回答明确问题再实现。当前优先候选是 30 天睡眠节律图，而不是为了好看先加通用 Dashboard。

## 7. PWA 与缓存

- `update-coordinator.js` 是 Service Worker 注册/更新唯一 owner；`app.js` 不得重复注册 SW。
- 页面、`home-config.json` 以及代码类资源在线时优先获取/重新验证网络版本，离线才回退缓存。
- 图标等稳定静态资源可 cache-first。
- 不允许业务代码更新依赖“开发者记得手工 bump cache name”才能生效。
- 主屏幕 PWA 图标属于安装级元数据，iPadOS 不保证已安装图标自动更新；不要为了图标更新冒数据丢失风险。

## 8. 仓库文档是产品的一部分

聊天记忆不是唯一事实源。重要决策必须落仓库。

### 必读文档

- `AGENTS.md`：产品原则、用户长期偏好、开发门禁；所有 Agent 第一入口。
- `ARCHITECTURE.md`：当前模块边界、数据/时间架构。
- `JSON_DATA_SCHEMA.md`：当前 JSON 对外协议唯一权威说明。
- `MAINTENANCE.md`：清理计划、临时兼容层、Git 历史策略。
- `TESTING.md`：验证策略。
- `SECURITY.md`：local-first / Web 安全边界。

### 文档维护规则

- 架构、数据协议、核心交互或发布机制变化时，同一轮必须更新对应文档。
- 不允许保留两个互相矛盾的“权威协议文档”。
- 旧文档若只会误导，应删除、合并或明确标记为历史，而不是因为已经存在就保留。
- README 只做仓库入口和文档导航，不复制大段规范。
- 代码是最终事实；一旦发现文档与运行代码冲突，先以代码确认现状，然后修正文档并增加可自动验证的契约。

## 9. 当前模块所有权

- `app.js`：Today / Profile / Context / 通用非 Sleep 记录编辑核心控制器；继续拆分，禁止重新吸收已外移职责。
- `record-templates.js`：吃奶 / 饮食待确认模板策略唯一实现。
- `history.js`：History 唯一控制器。
- `sleep-v3.js`：Sleep / Good Night / Good Morning / Wake night ownership / sleep projection。
- `timeline-v3.js`：流水时间排序 projection。
- `data-io-v3.js`：JSON/Excel 导入导出唯一实现。
- `record-model.js`：canonical temporal 唯一实现。
- `db.js`：持久化规范化边界。
- `update-coordinator.js`：PWA 更新协调唯一实现。
- `migration-v2.js` + `migration-v3.js`：历史安装升级链；不要仅因文件名旧而删除。

## 10. 修改方式

需求清楚时直接执行，不重复向用户确认已经明确的决定。

每轮尽量做到：

1. 判断问题属于 data truth / projection / UI / startup / cache / performance / docs 哪一层；
2. 只改拥有该职责的模块；
3. 删除被替代旧逻辑；
4. 补或更新自动检查；
5. 更新权威文档；
6. 确认 static-check + Pages 同一 HEAD 成功；
7. 再报告部署完成。