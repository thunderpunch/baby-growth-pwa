name: fix-sleep-metric-renderer-once

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/fix-sleep-metric-renderer-once.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply sleep metric renderer fix
        run: python scripts/fix-sleep-metric-renderer-once.py
      - name: Run full verification
        run: node scripts/verify.mjs
      - name: Commit fix and remove one-time tools
        run: |
          rm scripts/fix-sleep-metric-renderer-once.py
          rm .github/workflows/fix-sleep-metric-renderer-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix sleep metric renderer"
          git push origin HEAD:main
