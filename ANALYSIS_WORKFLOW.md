# 宝宝成长记录 — 数据分析与长期归档工作流

本文件定义宝宝私人数据、历史分析和长期分析上下文的权威归档方式。它解决的是“多年持续记录后仍能高效检索、复现历史结论、避免每次全量扫描”的问题。

## 1. 职责边界

- **PWA / IndexedDB**：日常事实产生端与本机工作副本。
- **Google Drive**：宝宝私人数据、年度主档、原始提交、历史分析与长期上下文的长期权威归档。
- **GitHub**：只保存代码、Schema、分析方法与 Agent 规则；禁止提交宝宝原始 JSON、分析报告、健康/医疗事实或 Drive 文件 ID。
- **ChatGPT 会话记忆**：只用于提升交流连续性，不是长期事实源。涉及历史事实或历史结论时优先读取 Drive 权威文件。
- **邮件**：可作为异地灾备，不作为主要检索入口。

## 2. Drive 逻辑目录

```text
宝宝成长记录/
├─ 00_索引与长期上下文/
│  ├─ archive-catalog.json
│  ├─ longitudinal-context.md
│  ├─ analysis-index-2026.json
│  ├─ analysis-index-2027.json
│  └─ ...
├─ 01_年度主档/
│  ├─ 2026/宝宝成长记录_2026.json
│  ├─ 2027/宝宝成长记录_2027.json
│  └─ ...
├─ 02_原始提交/
│  ├─ 2026/09/...
│  ├─ 2026/10/...
│  └─ ...
└─ 03_分析报告/
   ├─ 2026/09/...
   ├─ 2026/10/...
   └─ ...
```

规则：

- 年度主档按自然年分片；一个自然年只维护一个当前 canonical 主文件。
- 原始提交与分析报告按“年 / 月”分片，避免长期单目录累积数百或数千文件。
- `00_索引与长期上下文` 必须保持轻量，只存索引和浓缩上下文，不放原始大数据。
- 新自然年自动建立新年度主档，不把多年数据无限累加在一个生命周期 JSON 中。

## 3. 每次用户提交 JSON 后的标准流程

1. 校验文件是否为当前支持的 JSON Schema，并检查日期范围、稳定 ID、删除 tombstone、重复记录和 temporal 完整性。
2. 将用户本次原始文件以不可变副本保存到 `02_原始提交/YYYY/MM/`，文件名包含提交时间；原始文件不覆盖。
3. 读取对应自然年的 canonical 主档；若不存在则以本次有效数据创建。
4. 按当前合并规则幂等合并，生成该年度截至本次的完整 canonical JSON。
5. 更新 `01_年度主档/YYYY/宝宝成长记录_YYYY.json`；主档是正常分析的首要数据源。
6. 计算并记录主档 SHA-256、日期范围、record/day/profile 数量、最后更新时间等到 `archive-catalog.json`。
7. 根据用户问题进行分析；正常情况下只读取相关年度主档，不扫描所有原始提交。
8. 保存不可变分析报告到 `03_分析报告/YYYY/MM/`。
9. 更新当年 `analysis-index-YYYY.json` 和 `longitudinal-context.md`。
10. 如用户已启用邮件灾备，发送该自然年最新完整 JSON 快照；Drive 仍是主归档。

## 4. Canonical 年度主档

年度主档负责“当前事实”，不是分析历史。

文件命名：

```text
宝宝成长记录_2026.json
宝宝成长记录_2027.json
```

要求：

- 内容始终是该自然年截至最近一次合并的完整数据，而不是增量。
- 更新同一主文件，不通过无限新增 `final-v2-final2` 文件表达版本。
- 原始提交目录承担输入留档与可复现性，因此 canonical 主档允许原位更新。
- 年份关闭后，该年度主档原则上视为冻结；若后续补录旧年份，只修改对应旧年度，并更新索引。
- 跨年分析只加载真正涉及的两个或少数年度文件，不加载整个宝宝生命周期。

## 5. 原始提交的用途

`02_原始提交` 不是正常查询数据源，只用于：

- 复现某次分析当时的输入；
- 排查导出/合并异常；
- 验证某条事实何时首次出现；
- canonical 主档损坏时重建。

因此正常分析禁止为了“保险”每次扫描所有原始提交。

建议文件名：

```text
2026-09-04_1703_<exportId-short>.json
```

每份原始提交在索引中保存至少：文件名、Drive 文件引用、exportId、导出时间、数据日期范围、SHA-256、Schema version。

## 6. 分析报告与可追溯性

分析报告不可覆盖旧报告。

建议命名：

```text
2026-09-04_凌晨早醒分析.md
2026-09-21_三觉转两觉趋势.md
```

报告头部必须记录：

- analysisId；
- 分析时间；
- 用户问题 / 主题标签；
- 分析数据范围；
- 使用的年度主档及 SHA-256；
- 必要时使用的原始提交 exportId；
- 当时生效的 Profile version；
- 参考过的历史 analysisId；
- 主要结论；
- 置信度；
- 新增 / 更新 / 推翻的 hypothesisId。

历史报告保存“当时基于当时证据得到的结论”。后续数据推翻旧结论时，不改写旧报告，而是在新报告和长期上下文中记录结论演变。

## 7. 检索索引

### `archive-catalog.json`

只保存非常小的顶层目录信息，例如：

- 各年度 canonical 文件引用；
- 日期覆盖范围；
- 最后更新时间；
- SHA-256；
- records / days / profileVersions 数量；
- 对应年度 analysis index 文件。

其规模应接近 O(年份数)，即使记录 10～20 年也应很小。

### `analysis-index-YYYY.json`

每个自然年一个分析索引，避免未来数百份报告挤进单一索引。

每条索引只保存检索所需元数据，不复制整份报告：

```text
analysisId
analysisDate
title
tags
dataRange
sourceYear
sourceHash
summary
hypothesisIds
supersedes / challengedBy
reportFileRef
```

`summary` 应保持短小，用于判断是否值得进一步打开报告。

## 8. `longitudinal-context.md`

这是跨分析的“浓缩长期上下文”，不是日记，也不是第二份原始数据库。

只保留：

- 当前长期背景；
- 已较稳定的长期趋势；
- 当前仍在观察的假设；
- 近期重要阶段变化；
- 已推翻 / 已结束的关键假设及其结论；
- 指向具体 analysisId 的引用。

必须主动压缩：旧的重复描述应合并，不无限追加全文。目标是让它多年后仍能在一次读取中快速理解宝宝当前分析上下文。

建议把假设赋予稳定 ID，例如：

```text
H-SLEEP-001
H-FEED-003
```

状态使用：`观察中 / 支持增强 / 支持减弱 / 已确认趋势 / 已推翻 / 已结束`。

## 9. 正常分析的检索顺序

禁止默认“把 Drive 里所有宝宝文件全部读一遍”。标准流程：

1. 读取 `archive-catalog.json`；
2. 读取 `longitudinal-context.md`；
3. 根据问题确定主题、日期范围和需要的年度；
4. 只加载相应年度 canonical JSON；
5. 搜索对应年度 `analysis-index-YYYY.json`；
6. 通常只打开最相关的少量历史报告（例如 3～8 份）；
7. 只有需要复现/争议追踪时才打开原始提交。

这保证正常分析成本主要与“当前问题涉及的时间范围”相关，而不是与宝宝累计使用年限线性增长。

## 10. 长期承载与压缩策略

- 原始事实按年分片；原始提交和报告按年/月分片。
- 索引不保存报告全文或完整记录，只保存定位元数据和短摘要。
- 每年一个 `analysis-index-YYYY.json`，不要维护无限增长的全生命周期分析索引。
- `archive-catalog.json` 只指向年度索引，保持小型顶层目录。
- `longitudinal-context.md` 定期合并和淘汰重复描述，而不是不断 append。
- 视频、图片等大附件若未来加入，单独建立附件目录并只在记录/报告中保存引用，不嵌入 canonical JSON。
- 不使用 Git 历史保存宝宝个人数据版本。

## 11. 冲突与事实优先级

出现冲突时优先级：

1. 当前 canonical 年度 JSON 中的 factual records；
2. 对应的原始提交与稳定 ID / updatedAt / tombstone；
3. 当时生效的 Profile version；
4. 历史分析报告；
5. `longitudinal-context.md`；
6. ChatGPT 会话记忆。

分析结论永远不能覆盖原始事实。推断、趋势和假设必须与 factual data 分开。

## 12. 隐私规则

- GitHub 仓库不得保存宝宝真实记录 JSON、分析报告、健康/医疗事实、邮箱、Drive 文件 ID 或其它私人归档定位信息。
- Drive 目录和文件保持用户私有；只有完成用户请求所需时读取。
- 分析输出引用数据时尽量使用日期范围、analysisId、exportId/hash，而不是复制大量原始明细。
